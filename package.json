{
  "name": "modern-css-demos",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "browser-sync start --server --files '**/*.html, **/*.css, **/*.js' --no-notify --open",
    "start": "browser-sync start --server --no-notify"
  },
  "devDependencies": {
    "browser-sync": "^3.0.3"
  }
}
